<%= form_for(@basic) do |f| %>
  <% if @basic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@basic.errors.count, "error") %> prohibited this basic from being saved:</h2>

      <ul>
      <% @basic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :necessary_stuff %><br>
    <%= f.text_area :necessary_stuff %>
  </div>
  <div class="field">
    <%= f.label :how_to_make %><br>
    <%= f.text_area :how_to_make %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
